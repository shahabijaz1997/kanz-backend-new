= f.hidden_field :step, value: @step
= hidden_field_tag :step, @step
.form-group data-controller='image-preview'
  = render "file_field", form: f, field: 'shareholder_agreements', file: spv.shareholder_agreements
.form-group data-controller='image-preview'
  = render "file_field", form: f, field: 'property_deeds', file: spv.property_deeds
.form-group data-controller='image-preview'
  = render "file_field", form: f, field: 'loan_agreement', file: spv.loan_agreement
.form-group data-controller='image-preview'
  = render "file_field", form: f, field: 'service_provider_contracts', file: spv.service_provider_contracts
